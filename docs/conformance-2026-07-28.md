# Tachyon MCP — Protocol Conformance v2026-07-28 (Draft)

> **Status**: ⚠️ Model layer complete; runtime conformance untested (no conformance runner for draft spec yet)
> **Spec**: `modelcontextprotocol/docs/specification/draft` (2026-07-28)
> **Runner**: none — `@modelcontextprotocol/conformance` does not yet publish a draft build
> **Models**: generated by `ts2java.py` from `protocol/mcp-2026-07-28.ts`; all records carry `@JsonIgnoreProperties(ignoreUnknown = true)`

---

## 1. Draft vs Stable Delta

Changes introduced in the 2026-07-28 draft relative to 2025-11-25:

| Area | 2025-11-25 | 2026-07-28 Draft | Implementation Status |
|---|---|---|---|
| `Tool.execution` field | present | **removed** | ✅ generator omits it for draft models |
| `Tool.inputSchema` type | `{ $schema?, type:"object", properties?:{[k]:object}, required?:string[] }` | `{ $schema?, type:"object", [k:string]:unknown }` | ✅ `JsonNode` via field-level type-mapping |
| `Tool.outputSchema` type | same inline object shape | `{ $schema?, [k:string]:unknown }` | ✅ `JsonNode` via field-level type-mapping |
| `JSONObject` | absent | `{ [k: string]: JSONValue }` | ✅ mapped to `Map<String, JsonNode>` |
| `JSONArray` | absent | `JSONValue[]` | ✅ mapped to `List<JsonNode>` |
| `JSONValue` | absent | `JSONObject \| JSONArray \| string \| number \| boolean \| null` | ✅ mapped to `JsonNode` |
| `MetaObject` | absent | `{ [k: string]: unknown }` | ✅ mapped to `Map<String, JsonNode>` |
| Runtime protocol version | `2025-11-25` | `2026-07-28` | ❌ server still negotiates `2025-11-25` |
| `MCP-Protocol-Version` header | `2025-11-25` | `2026-07-28` | ❌ not updated in server/tests |

---

## 2. Model Generation Status

Generated by `ts2java.py --ts protocol/mcp-2026-07-28.ts --type-mapping protocol/mcp-2026-07-28_type-mappings.json` during `generate-sources` Maven phase.

| Type | Generated | Notes |
|---|---|---|
| `Tool` | ✅ | No `execution` field; `inputSchema`/`outputSchema` as `JsonNode` |
| `ToolCodec` | ✅ | `decodeValue`/`encodeValue` paths for `JsonNode` schema fields |
| `JSONObject` | ✅ | `Map<String, JsonNode>` |
| `JSONArray` | ✅ | `List<JsonNode>` |
| `JSONValue` | ✅ | `JsonNode` |
| `MetaObject` | ✅ | `Map<String, JsonNode>` |
| All other types | ✅ | 153 model classes generated |

All generated records carry `@JsonIgnoreProperties(ignoreUnknown = true)`.

---

## 3. Runtime Gaps

The server runtime (`tachyon-mcp`) is still aligned with 2025-11-25 at runtime. The following gaps exist for draft spec compliance:

| Gap | Location | Severity |
|---|---|---|
| Protocol version `2026-07-28` not negotiated | `InitializeHandler`, `ProtocolVersionHandler` | 🟠 HIGH — server rejects `2026-07-28` header with 400 |
| `MCP-Protocol-Version: 2026-07-28` header not sent in responses | `McpEndpointHandler` | 🟠 HIGH |
| `Tool.execution` field gated per negotiated version | `GatedToolCodec` | ✅ `GatedToolCodec` extends `ToolCodec`; omits `execution` when `session.protocolVersion() == "2026-07-28"` — but `2026-07-28` is not yet a negotiated version (see protocol version gap below) |
| Draft-only `JSONObject`/`JSONArray`/`JSONValue` types not used at runtime | — | 🔵 LOW — opaque types, no handler needed |

---

## 4. Pending

| Item | Blocker |
|---|---|
| Draft conformance runner availability | `@modelcontextprotocol/conformance` release |
| Add `2026-07-28` to accepted protocol versions | Runtime change in `ProtocolVersionHandler` |
| Version-conditional `Tool.execution` serialization | Runtime change in `ToolRegistry` + `ToolCodec` |
| Update e2e / conformance tests to send `MCP-Protocol-Version: 2026-07-28` | Test infra |
